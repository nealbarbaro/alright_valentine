$(document).ready(function(){

    // var refresh_timer
    // var userId = $('#message_window').attr("data-user");

    // var refresh_chats = function(){
    //   $.ajax({
    //     dataType: "script",
    //     type: "get",
    //     url: "/refresh/" + userId //create custom route
    //   });
    // }; // refresh_chats

    // var userId = $('.message-content').attr("data-user");

    // var start_intervals = function() {
    //   refresh_timer = setInterval(refresh_chats, 1000);
    // }; // start_interval

    // if ($('#message_window'.length)){
    //   start_intervals();
    // };

    // $(document).ajaxError(function() {
    //   clearInterval(refresh_timer);
    // });

  });