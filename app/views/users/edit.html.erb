<% if @user.subscription != nil %>
  <p>You are a Premium User with unlimited private messaging!</p>
<% else %>
  <p>Become a Premium User to have unlimited private messaging!  <%= link_to 'Subscribe Here', new_subscription_path %> </p>
<% end %>


<div class="row">
  <div class="large-6 columns">
    <%= render 'form' %>
  </div>
</div>



<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
